# Host extras

Instructions for every agent live in `../SKILL.md`. Files here are optional labels for a specific host.

- `openai.yaml` — display name and default prompt for Codex. Codex still reads `SKILL.md` for the actual method.
