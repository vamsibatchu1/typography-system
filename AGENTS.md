# Typography System

Follow [SKILL.md](SKILL.md). Keep `references/`, `scripts/`, `assets/`, and `agents/` next to it.

This folder is a portable Agent Skill. Install it by copying the whole `typography-system` directory into your host’s skills path (Cursor, Claude Code, Antigravity, Codex, or any other tool that loads `SKILL.md`).
